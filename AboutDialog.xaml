<Window x:Class="Drauniav.AboutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="{DynamicResource AboutTitle}"
        Width="460"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#F3F5F7">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="#D4D8DF" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconMaterial Kind="InformationOutline" Width="18" Height="18" Margin="0,0,8,0" Foreground="#374151"/>
                <TextBlock Text="{DynamicResource AboutHeader}" FontSize="14" FontWeight="SemiBold" Foreground="#111827"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" BorderBrush="#D4D8DF" BorderThickness="1" CornerRadius="8" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="{DynamicResource AboutVersionLabel}" Margin="0,0,8,8" Foreground="#374151"/>
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TxtVersionValue" Margin="0,0,0,8" Foreground="#111827"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="{DynamicResource AboutAuthorLabel}" Margin="0,0,8,8" Foreground="#374151"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TxtAuthorValue" Margin="0,0,0,8" Foreground="#111827"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="{DynamicResource AboutHomepageLabel}" Margin="0,0,8,8" Foreground="#374151"/>
                <TextBlock Grid.Row="2" Grid.Column="1" x:Name="TxtHomepageValue" Margin="0,0,0,8" Foreground="#111827"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="{DynamicResource AboutGithubLabel}" Margin="0,0,8,0" Foreground="#374151"/>
                <TextBlock Grid.Row="3" Grid.Column="1" x:Name="TxtGithubValue" Foreground="#111827"/>
            </Grid>
        </Border>

        <Button Grid.Row="2" Width="110" Height="34" HorizontalAlignment="Right" Margin="0,12,0,0" Click="BtnClose_Click">
            <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconMaterial Kind="Close" Width="16" Height="16" Margin="0,0,6,0"/>
                <TextBlock Text="{DynamicResource AboutClose}"/>
            </StackPanel>
        </Button>
    </Grid>
</Window>
