<Window x:Class="Drauniav.FfmpegMissingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="{DynamicResource FfmpegMissingTitle}"
        Width="620"
        SizeToContent="Height"
        MinWidth="620"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#F3F5F7">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="#D4D8DF" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconMaterial Kind="AlertCircleOutline" Width="18" Height="18" Margin="0,0,8,0" Foreground="#B45309"/>
                <TextBlock Text="{DynamicResource FfmpegMissingHeader}" FontSize="14" FontWeight="SemiBold" Foreground="#111827"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" BorderBrush="#D4D8DF" BorderThickness="1" CornerRadius="8" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                           Text="{DynamicResource FfmpegMissingIntro}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,10"
                           Foreground="#374151"/>

                <TextBlock Grid.Row="1" Grid.ColumnSpan="2"
                           Text="{DynamicResource FfmpegMissingInstallHint}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,10"
                           Foreground="#374151"/>

                <TextBlock Grid.Row="2" Grid.Column="0"
                           Text="{DynamicResource FfmpegMissingWingetLabel}"
                           Margin="0,0,8,8"
                           Foreground="#374151"/>
                <TextBox Grid.Row="2" Grid.Column="1"
                         Text="{DynamicResource FfmpegMissingWingetCommand}"
                         IsReadOnly="True"
                         BorderThickness="1"
                         BorderBrush="#CDD3DD"
                         Background="#F9FAFB"
                         Padding="8,4"
                         FontFamily="Consolas"
                         Margin="0,0,0,8"/>

                <TextBlock Grid.Row="3" Grid.Column="0"
                           Text="{DynamicResource FfmpegMissingHomepageLabel}"
                           Margin="0,0,8,0"
                           Foreground="#374151"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Foreground="#1D4ED8">
                    <Hyperlink x:Name="LnkFfmpegHomepage" RequestNavigate="LnkFfmpegHomepage_RequestNavigate">
                        <Run x:Name="TxtFfmpegHomepageValue"/>
                    </Hyperlink>
                </TextBlock>
            </Grid>
        </Border>

        <Button Grid.Row="2" Width="110" Height="34" HorizontalAlignment="Right" Margin="0,12,0,0" Click="BtnClose_Click">
            <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconMaterial Kind="CheckCircleOutline" Width="16" Height="16" Margin="0,0,6,0"/>
                <TextBlock Text="{DynamicResource FfmpegMissingClose}"/>
            </StackPanel>
        </Button>
    </Grid>
</Window>
